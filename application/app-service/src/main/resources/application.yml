server:
  port: 8081
springdoc:
  swagger-ui:
    path: "/doc/swagger-ui/index.html"
  api-docs:
    path: "/doc/api-docs"
spring:
  webflux:
    base-path: "/iasapi"
  application:
    name: event-management-challenge
  profiles:
    active: dev
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
logging:
  level:
    com.ias: DEBUG
    org.slf4j: DEBUG
aws:
  region: "us-east-1"
  secrets:
    rabbitmq-secret-name: "rabbitmq-secret-name"

security:
  jwt:
    key:
      private: a01aafbfe92bcef60384cb19fdb8812ad61504e95510f71b5a28c575721ef13c
    user:
      generator: AUTH_JWT_BACKEND